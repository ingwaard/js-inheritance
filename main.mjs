'use strict'
import { Rectangle as Rect, Circle, Triangle, Ellipse } from './graph.mjs'
import demo from './graph.mjs'

demo()
console.log(new Rect(), new Circle(), new Triangle(), new Ellipse())
process.exit()